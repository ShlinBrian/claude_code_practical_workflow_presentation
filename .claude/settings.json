{
  "worktree": {
    "baseRef": "head"
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'talks/.*/index.html'; then echo '⚠️  Talk HTML was modified. Run /sync-talk-notes to update notes.md'; fi"
          }
        ]
      }
    ]
  }
}
